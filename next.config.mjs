/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export', 
    basePath: '/react-first',  
    images: {
      unoptimized: true,  
    },
  };
  
  export default nextConfig;
  